<svg width="32" height="32" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
  <!-- Bola branca com sombra -->
  <defs>
    <radialGradient id="ballGradient" cx="0.3" cy="0.3" r="0.7">
      <stop offset="0%" stop-color="#ffffff" stop-opacity="1"/>
      <stop offset="70%" stop-color="#f0f0f0" stop-opacity="1"/>
      <stop offset="100%" stop-color="#d0d0d0" stop-opacity="1"/>
    </radialGradient>
    <filter id="shadow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="2" dy="2" stdDeviation="3" flood-color="#000000" flood-opacity="0.3"/>
    </filter>
  </defs>
  
  <!-- Círculo principal -->
  <circle cx="16" cy="16" r="12" fill="url(#ballGradient)" filter="url(#shadow)"/>
  
  <!-- Brilho -->
  <circle cx="13" cy="13" r="3" fill="#ffffff" opacity="0.6"/>
</svg>
